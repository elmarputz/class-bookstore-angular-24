import { BookFormErrorMessage } from './book-form-error-message';

describe('BookFormErrorMessage', () => {
  it('should create an instance', () => {
    expect(new BookFormErrorMessage()).toBeTruthy();
  });
});
